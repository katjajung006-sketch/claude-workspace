{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd /Users/katjajung/Desktop/claude-workspace-vorlage && git add -A && (git diff --cached --quiet || git commit -m \"Auto-save: $(date '+%Y-%m-%d %H:%M')\") && git push 2>/dev/null || true",
            "timeout": 30,
            "statusMessage": "Speichere Workspace auf GitHub..."
          }
        ]
      }
    ]
  }
}
